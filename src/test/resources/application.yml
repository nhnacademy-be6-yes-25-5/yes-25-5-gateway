server:
  port: 8085
spring:
  application:
    name: api-gateway
  profiles:
    active: dev
jwt:
  secret: ${JWT_SECRET}
eureka:
  client:
    enabled: false
    fetchRegistry: false
    registerWithEureka: false
  instance:
    preferIpAddress: true